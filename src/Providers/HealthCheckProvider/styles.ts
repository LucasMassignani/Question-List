import styled from 'styled-components';

export const Button = styled.button<{ variant?: 'error' }>`
  padding: 6px 12px;
  border-radius: 4px;

  background: #dc3545;
  border: 1px solid #dc3545;
  color: #ffffff;
`;
