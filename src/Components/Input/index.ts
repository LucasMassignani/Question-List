import styled from 'styled-components';

export const Input = styled.input`
  outline: none;
  padding: 6px;
  border: 2px solid rgb(239, 243, 244);
  border-radius: 5px;

  &:focus {
    border-color: #add8e6;
  }
`;
